@inject Microsoft.AspNetCore.Identity.SignInManager<ApplicationUser> _signIn

@if (_signIn.IsSignedIn(User))
{
<li class="nav-item dropdown">
    @*<a class="nav-link dropdown-toggle text-dark" href="#" id="navbarDropdown" roler="button"
       data-toggle="dropdown" aria-label="dropdown">Hello @User.Identity.Name</a>*@
    <a class="nav-link dropdown-toggle text-dark" href="#" id="navbarDropdown" roler="button"
       data-toggle="dropdown" aria-label="dropdown">Hello @(User.FindFirst("UserFirstName").Value +" "+ User.FindFirst("UserLastName").Value)</a>
    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
        <a class="dropdown-item" asp-action="logout" asp-controller="Account">Logout</a>
        <a class="dropdown-item" asp-action="changepassword" asp-controller="Account">Change password</a>
    </div>
</li>
        }
        else
        {
            <li class="nav-item">
                <a class="btn btn-outline-primary" asp-controller="Account" asp-action="login" asp-route-returnUrl="@Context.Request.Path">Login</a>
                <a class="btn btn-info" style="color:white" asp-controller="Account" asp-action="Signup">Register</a>
            </li>
        }

